diff --git a/drivers/mmc/core/Kconfig b/drivers/mmc/core/Kconfig
index f771bc3496a4..9740610fe762 100644
--- a/drivers/mmc/core/Kconfig
+++ b/drivers/mmc/core/Kconfig
@@ -26,3 +26,10 @@ config MMC_PARANOID_SD_INIT
 	  about re-trying SD init requests. This can be a useful
 	  work-around for buggy controllers and hardware. Enable
 	  if you are experiencing issues with SD detection.
+
+config MMC_UNIQUE_NUMBER
+	bool "Enable eMMC Unique Number generated by Kernel Driver"
+	default n
+	help	
+	  If you say Y here, the MMC layer makes Unique Number sysfs
+	  with the eMMC information to manage the device.
